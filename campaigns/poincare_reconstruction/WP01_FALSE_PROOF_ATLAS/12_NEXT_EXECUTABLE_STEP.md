# PC-WP01 — Next executable step

## Decision

The WP00-mandated false-proof classes have exact fixtures. WP01 is ready for joint integration with PC-WP02.

WP01 does not independently open PC-WP03. Its guards must first be attached to the corresponding positive theorem interfaces in the Hamilton–Perelman ledger.

## Immediate integration operation

For every node in `PC-WP02/06_DEPENDENCY_DAG.json`:

1. attach all relevant `PC-FP-*` guards;
2. verify that the source-normalized statement retains the hypotheses whose deletion triggers those guards;
3. confirm that the theorem consumer does not make a forbidden global, topological, or formalization inference;
4. record any mismatch as blocking proof debt.

## Required cross-checks

- `PC-L001` retains the external category bridge;
- `PC-L006` keeps pointed/local and quantitative canonical-neighbourhood semantics;
- `PC-L007` separates smooth segments, surgery times, local finiteness, and source corrections;
- `PC-L008` records topology mutation rather than metric-only modification;
- `PC-L010` does not imply `PC-C013` without `PC-L011` and `PC-L012`;
- `PC-L012` uses only the surgery-derived factor list;
- `PC-T017` labels every unformalized analytic import.

## Completion test

Joint WP01/WP02 integration is complete when:

- every positive theorem interface has attached adversarial guards;
- no fixture exposes a silent hypothesis deletion or scope expansion;
- the source-correction ledger discharges `PC-WP01-D005`;
- the remaining topology-event debt is transferred explicitly to PC-WP03;
- Agent Council and Referee review records agree on the next gate.

## Next-stage posture

After successful joint review:

- `PC-WP01` may be promoted as an eliminative atlas;
- `PC-WP02` may be promoted at theorem-interface reconstruction level;
- `PC-WP03` may open for finite surgery-history and extinction bookkeeping;
- no full analytic certification or new-proof claim is authorized.
