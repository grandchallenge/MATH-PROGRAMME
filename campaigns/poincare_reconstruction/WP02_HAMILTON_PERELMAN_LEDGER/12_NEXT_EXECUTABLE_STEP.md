# PC-WP02 — Next executable step

## WP02 decision

The Hamilton–Perelman route is reconstructed at theorem-interface level. Sources, corrections, parameters, consumers, adversarial guards, finite-extinction mechanism, proof debt, and certification boundaries are explicit.

WP02 is not a line-by-line independent proof of the analytic core. The following distinctions remain binding:

```text
source-normalized theorem interface != independently verified theorem
conditional terminal formalization != formalized Ricci-flow proof
all-time surgery flow != globally smooth flow
local finiteness != globally finite history
finite extinction != S3 without topology reconstruction.
```

## Joint WP01/WP02 review

The immediate operation is a cross-document review with these tests:

1. every `PC02-T*` node has all applicable `PC-FP-*` guards;
2. no source correction in the crosswalk lacks a downstream disposition;
3. the parameter registry agrees with the surgery theorem interface;
4. finite extinction is consumed only through `PC02-T017` and `PC02-T018`;
5. every claim distinguishes package proof, imported theorem, and formalization target;
6. remaining exact-source and analytic debts are nonblocking for the topology-history package but block independent-verification language.

## WP03 opening obligations

Before `PC-WP03` is promoted beyond initialization, it must discharge or explicitly inherit:

- `PC-WP01-D004`: bind surgery mutations to a finite-history schema;
- `PC-WP02-D006`: encode the complete topology transition list;
- `PC-WP02-D010`: attach a governing source/version to every event type and corrected interface.

The history schema must include:

```text
component identities and ancestry
separating versus nonseparating cuts
cap insertions
surviving and discarded components
permitted-factor expressions
local-finiteness evidence
finite-extinction terminal time
backward reconstruction certificate
malformed-history adversarial fixtures.
```

## Referee promotion criterion

WP01 and WP02 may be promoted when an independent review confirms:

- the atlas rejects the mandated false routes without overstating its power;
- the theorem ledger is source-faithful at its declared interface level;
- Perelman-I corrections are not silently reintroduced;
- topology and analysis are separated at surgery times;
- no part of the package claims a new proof or formal certification of the nonlinear analytic core.

## Authorized next stage after promotion

Open `PC-WP03 — surgery topology and extinction bookkeeping` with two parallel lanes:

- a human theorem reconstruction of every topology transition;
- a machine-readable finite-history schema and validator.

`PC-WP04` may continue only on the already authorized terminal algebraic/combinatorial certification slice. Full Ricci-flow formalization remains a future programme, not an implied consequence of this handoff.
