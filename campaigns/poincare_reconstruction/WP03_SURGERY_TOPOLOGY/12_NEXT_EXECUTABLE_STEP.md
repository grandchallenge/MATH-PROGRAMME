# PC-WP03 — Next executable step

## Referee disposition

`PC-WP03` is promoted as a conditional topology certificate.

The package closes the transferred obligations:

- finite event schema;
- source-bound transition catalogue;
- component ancestry;
- finite-history derivation;
- backward reconstruction;
- malformed-history validator;
- non-circular terminal group discharge.

## Next authorized stage

Initialize:

```text
PC-WP04 — bounded certification substrate
```

## Immediate obligations

1. define the formal `FactorAtom`, `Event`, and `History` types;
2. implement a total backward evaluator over finite histories;
3. prove active-set conservation and no-component-loss soundness;
4. prove separating/nonseparating evaluator clauses;
5. prove evaluator soundness conditional on the imported event relation;
6. implement the simply connected group-profile discharge;
7. replay all positive and malformed JSON fixtures;
8. emit a certificate that preserves source bindings and imported assumptions.

## Closed lanes

The following remain closed:

- formalization of Ricci-flow PDE estimates by assertion;
- treating the JSON schema as proof of event existence;
- removing source bindings from the formal theorem;
- claiming a new proof of Poincaré;
- using numerical topology recognition as a replacement for exact transition certificates.

## Re-entry conditions for analytic work

A future analytic-certification lane requires a separately scoped source-normalized theorem and library plan for canonical neighbourhoods, surgery existence, noncollapsing, or finite extinction. It may not be smuggled into WP04 through an opaque axiom named `perelman`.
