# OZ-RT-BZ-T3-014

Issue #953 admits the next source-declared T3 hypothesis class after the exact negative result of T3-013-A.

## New class

`SOURCE_DECLARED_RATIONAL_DELTA_COEFFICIENT_CLASS_001`

The protected reduced recurrence-residual target, support, harmonic alphabet, scalar namespace, primitive correction columns, and recurrence data are unchanged. The only widening is the coefficient law. Each protected correction column receives its own coefficient

```text
q_i(n,k,l) in Q(n,k,l)
```

and the response uses the true discrete product rule

```text
Delta(q_i M) = q_i(shifted point) M_shift - q_i(base point) M.
```

The class is source-motivated rather than a larger cutoff. The admitted upstream source explicitly records the next T3 campaign as a Delta-certificate / creative-telescoping class with coefficients rational in `(n,k,l)`:

```text
rain-1/-odd-zeta-values-moremath
commit 6cc0bf07137815ceeef0d9f340559f85352391e5
work/Z5T3_BRIDGE.md       blob 002c96d28123e5949c38656f26677ae5a723ee93
work/Z5CF_LINALG.md       blob 637ecaa7f3ee941a87932de390eb7336d7fde677
work/lb5/Qrow_rhosigma.m  blob 61f12f412726887f506e1d423b7ee183a22116e5
```

`Z5T3_BRIDGE.md` identifies rational Delta/creative-telescoping coefficients as the mapped pivot after the fixed-letter/local-residue calculus fails. `Z5CF_LINALG.md` supplies the finite `Q(n,k,l)` shift-module formulation and exact linear-algebra doctrine.

## Why this is genuinely new

T3-011-R closes only the frozen coordinate-zero Laurent monomial response algebra. T3-012-B closes the unchanged support-locked constant-coefficient class. T3-013-A widens that coefficient law only to the fixed source span `span_Q{1,rho,sigma}` and is refuted at the inherited necessary subsystem.

T3-014 instead admits the source-declared field `Q(n,k,l)` independently for every protected correction column. It does not change the target, support, harmonic alphabet, scalar namespace, recurrence, or correction channels. It introduces no polynomial-degree ladder and no denominator-family scan.

## First exact gate

The gate reuses exactly the two strict-interior sample points inherited from T3-012-B:

```text
(8,1,2)
(9,2,1)
```

For each protected correction column `i`, the producer introduces shared unknown values of `q_i` at every distinct base or shifted point used by this finite subsystem. Repeated points share the same unknown. The response is then assembled exactly from those values and the protected shifted harmonic monomials.

This is an exact restriction of the full rational class, not a relaxation. For any finite set of distinct rational points and prescribed rational values, multivariate polynomial interpolation gives a polynomial over `Q` with exactly those values. A polynomial is an element of `Q(n,k,l)`. Therefore every assignment represented by the finite value jet is realized by a member of the admitted global coefficient class. No hidden degree or denominator cutoff enters this gate.

The producer first replays T3-013-A and requires its exact negative result, including the embedded T3-012-B constant obstruction `84/85`. It then computes exact coefficient and augmented ranks of the rational-value-jet system in both pivot orders.

- If the enlarged finite system is inconsistent, the full declared rational coefficient class is refuted because every global rational solution would restrict to a solution of this necessary subsystem.
- If the ranks are equal, only the inherited obstruction is escaped. This does not construct a global rational function, a global Delta certificate, or a proof of the T3 identity. The next obligation is the source-declared global creative-telescoping solve over the finite harmonic shift module.

The independent verifier reconstructs the protected target and support through the T3-013-A verifier lineage, independently rebuilds the finite rational-value jet in reverse traversal order, rechecks the source declaration blobs, and recomputes the exact rank without importing the producer matrix.

## Claim boundary

Always retain:

```text
global_certificate_constructed = false
residual_sum_zero_proved = false
proof_effect = NONE
promotion_effect = NONE
t3_status = OPEN_WITH_CHARACTERIZED_BLOCKER
```

No T1-top, DEPTH, Sharp-12, primes `2,3`, formal, MATHCERT, companion/cuspidal, irrationality, infinitude, novelty, publication, patentability, deployment, product, or commercial claim is promoted by this gate.
