# T3-011-F runbook

The producer constructs the direct four-term mixed response from exact sequential single-axis shifts, translating every rational affine factor introduced by the first shift. It also checks the internal two-channel product-rule and reverse shift-order representations under the certified C-E semantic normal form.

The independent verifier reconstructs the frozen endpoint banks, active namespaces, and normalized witnesses through the reverse T3-010-C verification path. Its mixed-response authority is the two-channel product rule, with an independently implemented sequential shift action.

Run:

`python3 -m unittest tests.test_oz_rt_bz_t3_011_f`

The scan order is pair, endpoint, frozen candidate. Stop at the first semantic-functional ambiguity or first exact nonzero normalized cokernel pairing. A negative terminal requires complete exhaustion of the admitted class.
