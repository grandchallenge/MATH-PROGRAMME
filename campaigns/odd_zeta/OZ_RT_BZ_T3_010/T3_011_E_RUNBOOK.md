# T3-011-E runbook

The E producer and verifier are import-only campaign modules so the existing governed campaign replay registry is not widened. Repository policy executes them through `tests/test_oz_rt_bz_t3_011_e.py`.

The producer reconstructs the protected D/C source objects, candidate bank, active cells, and normalized witnesses. It derives the finite support-indexed moment coefficients and classifies the all-degree binomial sequence without constructing a degree-3-or-higher response.

The independent verifier reconstructs channel systems through the reverse T3-010-C verifier path, independently rebuilds the canonical witness and moment ledgers, and requires exact terminal agreement.

Run:

`python3 -m unittest tests.test_oz_rt_bz_t3_011_e`

Any semantic functional ambiguity terminates the audit fail-closed. Do not weaken representation invariance to obtain a closure result.
