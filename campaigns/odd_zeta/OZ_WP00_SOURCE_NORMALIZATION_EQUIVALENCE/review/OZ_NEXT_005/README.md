# OZ-NEXT-005 corrective sequence

This directory records the four corrective obligations opened by the OZ-NEXT-004 Referee review.

- `SHARP12_RECONCILIATION.yaml` — proof and certificate dependency reconciliation.
- `COMPUTATION_REPLAY_REGISTRY.yaml` — normalized finite replay lanes and environment capture.
- `PRIOR_ART_ACQUISITION.yaml` — versioned nearest-source identities and conservative theorem-scope findings.
- `LEAN_QUARANTINE.yaml` — declaration-level `sorryAx` boundaries.

The authoritative source revisions remain the two A004 source locks. These records do not complete `OZ-WP00`, authorize `NEW_AFTER_AUDIT`, or support a new irrationality conclusion.
