# OZ-NEXT-004 review package

This directory contains the machine-readable independent intake review for the A004 source locks.

Run:

```bash
python3 tools/validate_review_package.py
```

The package is deliberately fail-closed. It records a Referee rejection of promotion while preserving the admissible replay and semantic-audit results.
