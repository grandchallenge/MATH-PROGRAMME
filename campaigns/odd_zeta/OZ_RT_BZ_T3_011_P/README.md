# OZ-RT-BZ-T3-011-P — all-reciprocal trivariate Laurent cokernel audit

P is the final bounded sign-octant audit in the T3-011 Laurent-response ladder.

It admits exactly

`Delta_c Delta_d(x_c^{-r} x_d^{-s} x_e^{-t} G)`, `r,s,t >= 1`,

where `e` is the unique coordinate axis not represented by the active pair `(c,d)` and is unshifted by both finite differences.

## Protected intake

The immediate protected predecessor is T3-011-O:

- reviewed implementation head: `da9e2204edc452f39328ba88de54d73455ba9f32`;
- protected merge: `0065b997317d59a9c20f3fbff8792b37527b4a65`;
- terminal: `ACTIVE_SPECTATOR_DOUBLE_RECIPROCAL_TRIVARIATE_RESPONSE_CLASS_COKERNEL_INVISIBLE`;
- exact records: `2564/2564`;
- protected support-signature intersections: `0`.

P source-locks the protected O producer, contract, and verifier. It reuses N's exact signed tridegree solver but changes the spectator coordinate factor from forward to inverse. The independent verifier reconstructs the integer solver separately.

## Scope

P preserves the seven unordered channel pairs, endpoint banks, normalized witnesses, candidates, scalar namespace, support, harmonics, shell strata, coordinate specialization, and two finite-difference operators.

Only coordinate-zero poles are admitted. Shifted poles, arbitrary rational functions, partial fractions, support/harmonic/candidate widening, recurrence deformation, correction recombination, candidate linear combinations, and a third finite difference are outside scope.

The complete integer tridegree domain is derived from exact Laurent-factor signatures. No arbitrary degree cutoff is permitted. A recession direction is an admitted unbounded family only when a concrete protected witness/base support pair supplies a feasible seed.

The `t=0` face is checked against protected T3-011-I negative-negative pairwise semantics. The two active-degree-zero faces are retained as exact direct-response anchors only; they are not relabelled as previously certified trivariate classes.

## Terminals

- `ALL_RECIPROCAL_TRIVARIATE_RESPONSE_ESCAPE_FOUND`
- `ALL_RECIPROCAL_TRIVARIATE_RESPONSE_CLASS_COKERNEL_INVISIBLE`
- `ALL_RECIPROCAL_TRIVARIATE_RESPONSE_NOT_CERTIFIED__SEMANTIC_FUNCTIONAL_AMBIGUITY`
- `ALL_RECIPROCAL_TRIVARIATE_RESPONSE_NOT_CERTIFIED__CHARACTERIZED_BLOCKER`

## Claim boundary

This operation is a response-class cokernel audit only.

`residual_sum_zero_proved=false`; `proof_effect=NONE`; `promotion_effect=NONE`; `t3_status=OPEN_WITH_CHARACTERIZED_BLOCKER`.

Even exhaustive cokernel invisibility of this final Laurent sign octant does not prove T3, the residual-sum identity, Sharp-12, irrationality, infinitude, or any theorem/certification claim.
