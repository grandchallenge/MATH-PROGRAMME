# NS-CI-WP01 — Referee-promoted false-proof atlas

Campaign `NS-CI-001`; parent `MATH-PROGRAMME#55`; provider `grandchallenge/MATHFORGE#17`.

**State:** `REFEREE_PROMOTED_ROUTE_REJECTION_ATLAS`.

WP01 is eliminative. It proves only that fourteen specified routes fail in their stated forms. It does not refute universal critical integrability, imply blow-up, or validate a route merely because the route passes the atlas.

Protected target:

```math
\int_0^T\|u(t)\|_{L^6(\mathbb R^3)}^4dt<\infty
```

for every finite `T`, every datum in the full Fefferman rapid-decay class, and every associated Leray–Hopf solution.

The fixtures cover reversed time inclusion, the actual energy interpolation line, hidden uniform `H^1`, circular Grönwall closure, inadmissible `-\Delta u` testing, pressure-cancellation misuse, `N^4` Galerkin loss, `\varepsilon^{-4}` mollification loss, compact-support drift, numerical and formal trust-boundary overclaim, exponent-order drift, interior-to-global drift, and existential-to-universal drift.

Exact fixtures replay in MATHFORGE CI. FP-010 is limited to fixed finite-dimensional approximations without resolution-uniform verified bounds; verified a posteriori analysis and simulations used for falsification remain allowed. The atlas is extensible rather than exhaustive.

A triggered fixture requires route termination or narrowing and an explicit new scale-compatible, non-circular estimate. Passing the atlas is not a proof certificate.

WP00/WP01/WP02 and MATHCERT consistency review is complete. Verifier, Adversary, Amanuensis, and Referee approve promotion. There is no blocking review debt. Merge WP01 governance before WP02; WP02 carries the combined final ledger.
