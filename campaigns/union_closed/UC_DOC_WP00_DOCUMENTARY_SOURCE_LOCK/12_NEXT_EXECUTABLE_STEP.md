# First executable step

## Input

- repository-only source pointer at `campaigns/union_closed/UC_DOC_WP00_DOCUMENTARY_SOURCE_LOCK/artifacts/the_element_in_half_the_worlds.tex`;
- source-lock authority at `campaigns/union_closed/UC_DOC_WP00_DOCUMENTARY_SOURCE_LOCK/artifacts/UC-DOC-WP00_SOURCE_LOCK.json`;
- public candidate metadata in `docs/documentaries/DOCUMENTARY_CANDIDATES.json`;
- canonical claim ledger and centrally registered Agent Council review;
- compiled monograph and illustrated bundle identities;
- Poincaré reference reader substrate and BSD full-tier exemplar;
- manifest-driven admitted and candidate documentary validator.

## Operation

Open `UC-DOC-WP01` and construct one atomic admission change containing:

1. `docs/documentaries/union_closed.edition.json`;
2. `docs/documentaries/union_closed.md`;
3. `docs/assets/documentaries/union_closed/` with topic-specific native web plates;
4. an admitted source record at `docs/documentaries/sources/the_element_in_half_the_worlds.tex` copied or moved from the repository-only pointer with unchanged locked identities;
5. one `ARTIFACT_MANIFEST.json` volume entry with:
   - `documentary_tier: full`;
   - `claim_status: open`;
   - `problem_class: open_conjecture`;
   - reader-facing `display_status`;
6. collection-index and MkDocs navigation entries;
7. removal of the `union_closed` candidate from `DOCUMENTARY_CANDIDATES.json` in the same change;
8. problem-specific mathematical-spine tests.

## Output artifact

A draft Union-Closed full-tier web edition whose source, status, sections, assets, release identities, and claim boundary are bound to UC-DOC-WP00 without retaining simultaneous candidate and admitted membership.

## Completion test

The generalized documentary validator, canonical claim-ledger and review registries, repository unit tests, strict MkDocs build, accessibility checks, and all global policy and formal replays pass with:

- no theorem strengthening;
- no candidate/admitted overlap;
- no orphaned page, edition record, source record, asset, asset directory, candidate lock, or static file;
- no false Millennium classification;
- no public source pointer before the atomic admission commit.

## Node advanced

`UC-DOC-S011`.
