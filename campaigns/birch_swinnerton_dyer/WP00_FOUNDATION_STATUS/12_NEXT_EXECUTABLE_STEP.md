# BSD-WP00 — Next executable step

## Promotion record

The WP00 promotion preconditions are discharged:

1. repository schema, policy, and documentation checks passed in workflow `30083374165`;
2. independent Referee reconstruction found no blocking normalization or implication error;
3. Amanuensis cross-document integration found no conflict.

These are governance determinations, not mathematical evidence for BSD.

## Immediate execution

Open exactly two parallel packages.

### `BSD-WP01` — false-proof atlas

Build executable semantic fixtures for at least:

- root-number sign interpreted as exact rank;
- numerical near-zero interpreted as exact vanishing;
- Selmer corank interpreted as Mordell–Weil rank;
- hidden assumption of finite `Sha[p^infty]`;
- a one-prime valuation identity promoted to the strong formula;
- a `p`-adic order promoted to a complex order without a comparison theorem;
- rank-one Euler-system logic extrapolated to higher exterior rank;
- family, density, or finite-database evidence promoted to universality;
- primitive/imprimitive Euler-factor drift;
- exceptional-zero correction omitted.

Completion test: every fixture contains the invalid argument, the smallest exact obstruction, the corrected statement, and a regression assertion.

### `BSD-WP02` — source-normalized theorem ledger

For every admitted theorem, record:

1. exact curve and field class;
2. analytic-rank range;
3. prime and reduction hypotheses;
4. residual representation hypotheses;
5. direction of implication;
6. rank, Selmer, `Sha`, parity, valuation, or leading-term conclusion;
7. complete/imprimitive and complex/`p`-adic normalization;
8. exceptional factors and excluded primes;
9. theorem number and primary source;
10. formalizable interface and remaining proof debt.

Completion test: no result is represented by author names or a theorem slogan alone.

## Closed stages

Until WP01 and WP02 are integrated and Referee-promoted, do not open:

- broad mechanism generation;
- computational searches presented as evidence for the universal conjecture;
- novelty claims;
- a preferred restricted theorem target;
- a higher-rank Euler-system programme that has not identified its exact missing determinant or exterior-power obligation.

## Spine nodes advanced

WP01 advances `BSD-B080` by protecting the implication boundary. WP02 advances `BSD-B090` by replacing compressed theorem interfaces with source-normalized records. Restricted-target selection begins only at the later Council gate.
