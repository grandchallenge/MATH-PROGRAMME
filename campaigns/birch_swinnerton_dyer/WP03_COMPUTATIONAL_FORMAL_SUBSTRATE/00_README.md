# BSD-WP03 — Computational and formal substrate

Campaign `BSD-001`; state `REFEREE_PROMOTED_SUBSTRATE`.

WP03 creates a governed substrate for three activities that must never be conflated:

1. **individual-curve certification** — a bounded claim about one explicitly identified elliptic curve;
2. **finite database experimentation** — descriptive, falsifying, or hypothesis-generating analysis over a frozen finite population;
3. **formal interfaces** — algebraic and logical lemmas that can be certified without assuming BSD.

The package supplies machine-readable schemas, positive and adversarial fixtures, a deterministic replay, a claim-promotion policy, a formal-interface registry, theorem-ledger coupling, and an exact CI gate. It proves no instance of BSD and contains no certified curve result.

Run:

```bash
python campaigns/birch_swinnerton_dyer/WP03_COMPUTATIONAL_FORMAL_SUBSTRATE/replay.py
```

The replay checks that:

- a certificate cannot be universal, family-wide, singular, or numerically supported only;
- every certificate import resolves to a certificate-composable WP02 theorem record with evidence-bearing hypothesis verification;
- a database experiment binds a content-addressed finite snapshot, explicit query, outputs, and exact population count;
- no finite dataset or allowed-edge path can discharge `BSD-RANK-Q`, `BSD-SHA-Q`, or `BSD-LEAD-Q`;
- formal interfaces cannot import an open BSD statement through any assumption field;
- incomplete and complete \(L\)-function normalizations remain distinct;
- one-prime information cannot be promoted to a global leading-term identity;
- WP02 theorem-ledger changes trigger WP03 replay;
- WP04 target selection remains closed.

Independent Referee reconstruction and the pre-promotion gates passed in dedicated workflow `30138932356` and Programme workflow `30138932345`.

Passing WP03 validates the substrate contract only. It is not evidence for BSD, a mechanism, novelty, a certified curve, or a restricted target. WP04 is next eligible but remains unauthorized.
