name: CMDG condensed CM1

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cmdg-cm1-promotion-${{ github.ref }}
  cancel-in-progress: false

jobs:
  replay:
    uses: ./.github/workflows/cmdg-formal-lane-replay.yml
    with:
      lane: cmdg-cm1
