name: CMDG vertical spine V0

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cmdg-vertical-spine-v0-promotion-${{ github.ref }}
  cancel-in-progress: false

jobs:
  replay:
    uses: ./.github/workflows/cmdg-formal-lane-replay.yml
    with:
      lane: cmdg-vertical-spine-v0
