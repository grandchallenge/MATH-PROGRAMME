name: CMDG Euclid bridge

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cmdg-euclid-bridge-promotion-${{ github.ref }}
  cancel-in-progress: false

jobs:
  replay:
    uses: ./.github/workflows/cmdg-formal-lane-replay.yml
    with:
      lane: cmdg-euclid-bridge
