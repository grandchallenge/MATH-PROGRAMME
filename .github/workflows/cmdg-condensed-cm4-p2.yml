name: CMDG condensed CM4 P2

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cmdg-cm4-p2-promotion-${{ github.ref }}
  cancel-in-progress: false

jobs:
  replay:
    uses: ./.github/workflows/cmdg-formal-lane-replay.yml
    with:
      lane: cmdg-cm4-p2
