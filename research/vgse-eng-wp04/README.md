# VGSE-ENG-WP04 — geometry-to-quotient coupling and identifiability

**Campaign:** `VGSE-001`  
**Governed work:** `VGSE-GEOMETRY-QUOTIENT-COUPLING-001`  
**Protected start:** `fb34625c756d5500a63478a11e9f1d2dc89c722e`  
**Disposition:** terminal candidate — `GEOMETRY_QUOTIENT_NON_IDENTIFIABLE`

## Question

Does the qualified C05 geometric-realization evidence identify a mathematical coupling

[
Phi:mathcal G	omathcal Q,qquad mathcal Q=(mathbb R_{>0})^8,
]

to the eight protected WP01/WP02 quotient coordinates?

## Core result

Not under the currently qualified evidence.

The obstruction is structural and evidentiary, not merely a failed regression.

1. C05 certifies five distinct generated planar t-embedding branches.
2. The governed reconstruction loads one graph-weight fixture and then generates all five branches from the five algebraic witnesses.
3. Therefore the five apparent geometry/quotient pairs all reuse one quotient point. They are reconstructed correspondences, not five independent experiments.
4. The independent visible Figure-16 source geometries do not supply the missing quotient variation. The protected C06 producer audit rejects the obvious Euclidean dual-edge-length bridge to pinned `C` by a factor greater than 131.5 even after all boundary relabelings and both color/complement conventions.
5. The retained t-embedding primitive has edge increment
   `f(white) * sign_e * weight_e * f_tilde(black)`.
   Geometry therefore exposes products of the weight with branch-dependent vertex factors. No protected derivation currently eliminates or reconstructs those nuisance factors from geometry alone.

Accordingly, no nontrivial general `Phi`, no quotient subset, no `D Phi`, and no bounded latent metric augmentation is identified.

This is not a theorem that no such coupling can exist. It is a precise statement that current qualified evidence does not identify one.

## Geometry model

`GEOMETRY_MODEL.json` defines the admitted geometry narrowly:

- one fixed prescribed boundary gauge;
- three labeled interior dual vertices `A,C,D`;
- eight fixed face cycles;
- five certified isolated branches;
- exact/interval geometric constraints inherited from C05;
- no silent quotient by scale, reflection, relabeling, or an undeclared geometric equivalence group.

The five visible source drawings remain distinct source-derived geometry records. The protected source-equivalence audit does not identify them with the five generated branches by coordinate identity.

## Pairing audit

`EVIDENCE_MATRIX.json` records:

- reconstructed generated pairs: **5**;
- genuinely independent geometry/quotient pairs: **0**;
- independently varied quotient points: **0**;
- visible source geometries: **5**;
- visible source geometries with authorized pinned-C quotient pairing: **0**.

A function that interpolates these records would therefore manufacture information absent from the evidence.

## Hypotheses

### H0 — no identifiable coupling

Supported as the current evidentiary disposition.

### H1 — direct Euclidean length rule

Falsified in the protected producer audit.

The test uses Euclidean dual-edge lengths from each visible source-vector drawing, direct almost-perfect-matching boundary measurement, free projective scale, all `6! = 720` boundary relabelings, and both global color/complement conventions.

Retained bounds:

- minimum observed best distortion: `131.50098551816274`;
- certified rounding-robust lower bound: `131.50058917470795`;
- required separation threshold: `100`.

### H2 — dimensionless geometric invariants

Not identifiable. The geometry varies across the five generated branches while the audited quotient observation does not vary. No second independent quotient point exists to distinguish a predictive invariant from an incidental branch descriptor.

### H3 — low-dimensional smooth coupling

The Jacobian is **not identifiable**. It is not reported as rank zero. With only one observed quotient point, no empirical derivative of `Phi` is determined.

### H4 — latent-variable coupling

Not warranted. Free latent variables would absorb the missing correspondence. A future latent variable must have independently grounded provenance, bounded dimension, and testable role.

## Why WP02/WP03 are not replayed as coupling evidence

WP02 and WP03 already give a global quotient-response inverse and exact response-manifold feasibility relations.

No geometry-to-quotient predictor survives WP04. Propagating the already known pinned quotient through WP02/WP03 would therefore be circular. The downstream map remains protected and available, but it cannot substitute for evidence of `G -> Q`.

## Deterministic replay

From this directory:

```bash
python analyze_wp04.py --check
python test_wp04.py
```

The replay checks the evidence counts, single-quotient obstruction, H1 negative threshold, hypothesis dispositions, claim boundaries, and terminal outcome.

## Claim boundary

WP04 does not establish:

- `VGSE-C06`;
- Figure-16-to-pinned-C source correspondence;
- physical stiffness or mechanical advantage;
- rigid foldability;
- collision freedom;
- finite-thickness feasibility;
- material, friction, or hinge behavior;
- manufacturability or tolerance performance;
- fatigue or durability;
- novelty, priority, patentability;
- product performance or commercial value.

A mathematical graph weight remains a mathematical graph weight.

## Reopening

See `NEXT_ACTION.md`.

No WP05 is automatically authorized.
