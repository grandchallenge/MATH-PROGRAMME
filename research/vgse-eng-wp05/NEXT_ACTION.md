# VGSE-ENG-WP05 — terminal next action

Current terminal candidate:

`CYCLE_INVARIANT_QUOTIENT_RECONSTRUCTION_PARTIAL`

## What WP05 established

The original cancellation idea works exactly, but only on the closed-cycle part of the graph.

There are eight independent gauge-invariant weight directions. Graph topology splits them into:

- three closed cycles;
- five boundary-to-boundary paths.

The three cycle monomials cancel every discrete-holomorphic vertex factor and can therefore be read directly from embedding edge increments.

The five path monomials retain branch-dependent boundary endpoint factors. When those factors are supplied from the certified algebraic witness, the full eight-dimensional quotient is recovered exactly and is unimodularly equivalent to the WP01 chart.

## Why WP05 stops

WP05 has not derived the five boundary factors from geometry alone.

Using the already-retained algebraic witness to supply them verifies the factorization and full calibrated reconstruction, but it is not a new geometry-only inverse. Continuing to fit or infer the factors from the same five branches would recreate the interpolation problem WP04 prohibited.

## Reopening input

A successor should open only for one sharply bounded question:

> Can the five boundary factors be reconstructed canonically from admitted embedding geometry without consuming the algebraic branch witness, the known graph-weight quotient, or a source-correspondence assumption?

A valid route must expose a structural identity or an independently varied test, not a lookup table over the five retained branches.

## Protection sequence

Before WP05 is complete:

1. deterministic replay on the exact candidate head;
2. read-only Adversary review;
3. read-only Referee review;
4. continuity rebind to the exact reviewed head if required;
5. required checks and native merge queue;
6. protected-main readback;
7. terminal checkpoint update and tracker closure.

No further VGSE work package is automatically authorized.
