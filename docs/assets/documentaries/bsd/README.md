# BSD web plates

These lightweight native SVG plates support the browser-native edition of *The Hidden Music of Elliptic Curves*.

They are classified as **pedagogical orientation only**. The semantic HTML, browser-rendered equations, source record, campaign artifacts, and explicit claim-boundary panels govern every mathematical statement.

The original rendered PDF and complete illustrated source bundle retain their checksum-locked release identities in `docs/documentaries/ARTIFACT_MANIFEST.json`.
