# <WORKSET-ID> — Handoff

## Purpose

Target work repository: `<OWNER/REPOSITORY>`

INTELLECT work-package phase: `<Charter | Generation | Specification | Realization | Confrontation | Judgment | Integration | Disposal | Complete | not applicable>`

State the work-set purpose in one short paragraph. Preserve the governed purpose; do not silently redefine it during realization.

## Primary deliverable

State the one primary artifact this handoff is trying to advance.

## Material acceptance criteria

List the inherited conditions that make the work set materially complete.

- criterion 1
- criterion 2
- criterion 3

Do not weaken or rewrite governed acceptance criteria merely to match the current implementation.

## Current substantive state

State only the current domain-relevant state of the primary artifact.

Include:

- what exists;
- what is incomplete;
- what is known to be wrong or uncertain;
- what has already been substantively verified.

Exclude procedural motion unless it remains materially relevant. Do not infer an INTELLECT phase transition or `Complete` state from local progress alone.

## Authoritative pointers

List only the live authoritative files or locations needed to execute safely. Point to authority; do not duplicate its text.

Default authority pointers:

- `grandchallenge/INTELLECT:CONSTITUTION.md`
- `grandchallenge/INTELLECT:governance/constitutional_authority_schedule.json`
- `grandchallenge/MATH-PROGRAMME:handoffs/README.md`
- `<TARGET WORK REPOSITORY>:AGENTS.md` when present
- `<target-domain-authoritative-file-1>`
- `<target-domain-authoritative-file-2>`
- `<issue / PR / protected branch / canonical document if actually needed>`

Include an effective INTELLECT amendment, staffing directive, admitted standard, certification route, or other domain instrument only when it materially governs this work set.

## Smallest safe next tranche

State the next bounded unit of work that should be executed first.

It should be small, materially useful, directly connected to the primary deliverable, and lawful within the current INTELLECT phase and target-domain authority.

## Material dependencies and boundaries

List only dependencies or boundaries that materially constrain execution.

Examples:

- protected branch or canonical source of truth;
- current INTELLECT phase or exact phase-transition gate;
- required office finding or exact-subject review mode;
- target repository's delegated domain authority;
- MATHCERT certification boundary;
- AETHER production-semantic boundary;
- external dataset or document dependency;
- domain-specific theorem/proof boundary.

Do not infer authority from the fact that this handoff is stored in `MATH-PROGRAMME`.

## Reserved authority / stop conditions

State the exact cases where the execution lead must stop and escalate. Cite the controlling instrument for each reserved transition.

Examples:

- exact Human Steward power reserved by INTELLECT Article X or the live authority schedule;
- exact domain-specific reserved approval transition;
- material scope or work-package contract change;
- contradiction of a governing claim;
- failure of a central theorem or experiment assumption;
- blocked required dependency.

Do not request approval merely because a transition is important or polished; distinguish required office findings from genuinely reserved human authority.

## Notes intentionally omitted

This handoff intentionally omits:

- constitutional law and authority schedules, inherited by reference from `grandchallenge/INTELLECT`;
- general operating doctrine inherited by reference;
- historical process detail not needed for present execution;
- large procedural evidence already preserved elsewhere;
- duplicated governance instructions.
